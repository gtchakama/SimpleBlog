<!-- app/views/posts/show.html.erb -->
<div class="container mt-5">
  <h1><%= @post.title %></h1>
  <p><%= @post.content %></p>
  <p><%= link_to 'Edit', edit_post_path(@post), class: 'btn btn-secondary' %></p>
  <p><%= link_to 'Back', posts_path %></p>
</div>
